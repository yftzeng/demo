{
    "config": {
        "bin-dir": "vendor/bin",
        "preferred-install": "dist",
        "cache-dir": "vendor/_cache"
    },
    "autoload": {
        "classmap": [
            "src/"
        ]
    },
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^9"
    },
    "minimum-stability": "dev"
}
